# ══════════════════════════════════════════════════════════════════════
# 本地环境变量模板 —— 复制为 .env 后填入真值
#
#     cp .env.example .env
#
# .env / .env.local 已在 .gitignore 里，**不会**进版本库。
# 本文件只放占位符，任何真实密钥都不许写进来（它是要提交的）。
#
# 变量名与 5 级 provider fallback **与 ServBay 全家桶的翻译脚本保持一致**
# （ServBay-Windows/.env.example、servbay-support/scripts/.env.example、
#  ServBay-macOS/Scripts/.env.example），同一份 .env 可以跨仓库复用。
# ══════════════════════════════════════════════════════════════════════

# ── i18n 批量翻译（firmware/scripts/translate_catalog.py）────────────
# 用途：给 firmware/scripts/i18n_catalog.py 补齐新语言的译文。
# 详细用法见 firmware/scripts/README.md。
#
# 5 级 LLM Provider Chain（前一级额度耗尽/限流后自动切下一级）：
#   1. 智谱 GLM Coding Plan (glm-5.2)  ← 主力，翻译质量最好（付费订阅）
#   2. Google Gemma 4-31b-it           ← 免费 + RPD 宽；输出的 <thought> 段自动剥离
#   3. Google Gemini 3.5-flash         ← 免费但 RPD 严；质量顶级
#   4. Google Gemini 3.6-flash         ← 免费但 RPD 严；质量好、更快
#   5. 智谱 GLM 4.5-flash（免费）      ← 最终兜底
#
# 缺 key 的级别自动跳过——只填 ZHIPU 就用第 1、5 级；只填 GEMINI 就用第 2、3、4 级。
# 两个都不填也能跑 `--dry-run`（只扫描缺哪些译文，不发任何请求）。

ZHIPU_API_KEY=<YOUR_ZHIPU_KEY>          # 智谱：https://open.bigmodel.cn/usercenter/apikeys
GEMINI_API_KEY=<YOUR_GEMINI_KEY>        # Google Gemini：https://aistudio.google.com/apikey

# ── 可选参数（都有合理默认值，通常无需修改）──────────────────────────
# LLM_TIMEOUT=300                       # 单次 API 超时（秒）
# LLM_MAX_TOKENS=8192                   # 单次输出上限；一批 25 条短词条够用
#                                       # （ServBay 那几份默认 16384，它们翻整篇文档）
#
# 各级模型覆盖：
# ZHIPU_CODING_MODEL=glm-5.2
# GEMMA_MODEL=gemma-4-31b-it
# GEMINI_TIER2_MODEL=gemini-3.5-flash
# GEMINI_TIER3_MODEL=gemini-3.6-flash
# ZHIPU_FREE_MODEL=glm-4.5-flash
#
# 各级端点覆盖（默认值写在脚本里；换成别的 OpenAI 兼容网关即可统一计费）：
# ZHIPU_CODING_BASE_URL=https://open.bigmodel.cn/api/coding/paas/v4
# ZHIPU_FREE_BASE_URL=https://open.bigmodel.cn/api/paas/v4
# GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/
#
# 想让第 2/3/4 级统一走本机 ServBay AI Gateway（需先在 ServBay 里启动该服务）：
#   GEMINI_BASE_URL=http://127.0.0.1:11580/v1
#   GEMINI_API_KEY=<网关签发的虚拟 key，不是 Google 的原始 key>
