#!/usr/bin/env python3
"""板级元数据的**唯一来源**。改版本号只改这一个文件。

## 为什么单独抽出来

这块板在「改了一处、漏了另一处」上栽过 5 次了（板厚、层数、孔径、版本号……）。
症状每次都一样：**自研的复核脚本全绿，但板子是错的**——因为复核脚本读的是
它自己那份副本，而 Gerber 用的是另一份。

版本号原来有三份写死的：

    gen_silk.py:32          BOARD_REV = "V3.2"
    gen_brand_silk.py:43    BOARD_REV = "V3.2"     ← 注释还写着「改那边记得同步这边」
    gen_assembly.py:204     文档标题里的 "V3.2"

「记得同步」这种约定就是给未来的自己挖坑。改成 import 之后，漏改在物理上
不可能发生。

## 版本号规则

2026-08-20 评审：「版本号你要 +0.1」——每次改动实际影响到板子的**制造输出**
（Gerber / 钻孔 / 丝印）就 +0.1。改脚本、改文档不算。

历史：
  V1.0  → V3.1   最初写成 1.0 是错的，这块板承接的是 v3 系列
  V3.1  → V3.2   过孔改 0.45/0.3，用上嘉立创免费孔径档
  V3.2  → V3.3   修 4 对位号丝印互换（R33/C25、C46/C47、L14/C30、C18/C6）
  V3.3  → V3.4   D2/D3 补阴极标记线（官方 0402 封装只给了个 r=0.05mm 的圆点，
                 实物上认不出方向）。V3.3 的 Gerber 已生成过，不升号会出现
                 同一版本号对应两份不同 Gerber——这正是版本号要防的事
  V3.4  → V3.5   1090 板载 IFA 三处改动（V3.4 实板 VNA 实测驱动，详见
                 gen_ifa_footprint.py 文件头）：辐射臂加长 5.8mm、短路脚改成
                 引线+隔离槽（实测 R 25.79→57.56Ω 的因果已闭合）、开路端净空
                 1.0→3.95mm。天线整体左移 6mm 腾空间，ANT_TOP 区右边界跟着退让
  V3.5  → V3.6   固化最终人工布局、走线与重建快照；此前只改了 PCB 丝印，遗漏了
                 本元数据源和公开文档，V3.7 修订时一并收敛
  V3.6  → V3.7   GNSS 有源天线 bias Gate 修正：Q4→A/V1、Q5→B/V2，并重布
  V3.7  → V3.8   QPL9547 pin7 馈电/pin1 偏置网络补齐；BNO085 旋转与 v4 对齐
                 两条控制线；RF 路径、bias tee、器件位置和参考面不变
  V3.8  → V3.9   二次硬件审计 ECO：比较器使能、检波耦合、核心/射频去耦、
                 CC1312R 复位上拉、USB ESD 与 VBUS 检测
  V3.9  → V3.10  板载1090MHz IFA按V4.0实板装盒VNA结果定型：铜箔外包络
                 50.0mm（中心线48.5mm），取代53.5mm画长待切版本
"""

BOARD_NAME = "Pilot Kit Avionics Board"
BOARD_REV = "V3.10"
BOARD_DATE = "2026-09"
COPYRIGHT = "(c) Pilot Kit"
WEBSITE = "https://air.club"
SUBTITLE = "1090 / 978 ADS-B  +  GNSS  +  IMU  +  BARO"

# ⚠️ **文件名不含小版本号，跟 BOARD_REV 是两回事**，别跟着版本号改。
#
# 2026-08-20 之前叫 expansion-board-v3.1，而丝印上的版本号已经走到 V3.2/V3.3，
# 目录名和版本号对不上，看着就像有两块不同的板。这次统一成 v3：
# 目录、KiCad 工程、库名、Gerber 输出名全部用它，**版本迭代只改 BOARD_REV**。
#
# 改这个名字要动 71 个文件、308 处引用，包括 sym-lib-table / fp-lib-table 的
# 库名、原理图里每个 `expansion-board-v3:符号名`、kicad_mod 的 3D 模型路径、
# 还有 .gitignore 里的 fab/ 路径。别轻易再改。
PCB_BASENAME = "expansion-board-v3"

# 板框。丝印居中要用这个算，别在各脚本里各写一份。
#
# 这里同样出过事：gen_brand_silk.py 原来写死 `CX = 100.05`，注释是「板宽 100.1mm
# 的中线」——算错了，板框是 49.95~150.05，中线是 100.00 不是 100.05。
# 偏 0.05mm 丝印上根本看不出来，但它让**幂等失效**：gen_brand_silk 按坐标删
# 上一轮的文字，而 gen_silk 生成时用的是 (X0+X1)/2 = 100.00，两边对不上，
# 于是旧的删不掉、新的又加一份，板级丝印在同一处叠了两层。
X0, Y0, X1, Y1 = 50.0, 50.0, 150.0, 112.0
CX = (X0 + X1) / 2                   # = 100.0，与板框实测中线一致
