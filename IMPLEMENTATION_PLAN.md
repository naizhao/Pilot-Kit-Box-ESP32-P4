# 迁移至 4.3″ 触摸屏 + LVGL — 实施计划

**分支**：`feat/lcd-4.3-touch`
**设计依据**：[`docs/ux/box-4.3-ux-spec.md`](docs/ux/box-4.3-ux-spec.md)（文字规格）
+ [`docs/ux/box-4.3-ux-spec.html`](docs/ux/box-4.3-ux-spec.html)（视觉稿，浏览器直接打开）
**硬件**：微雪 ESP32-P4-WIFI6-Touch-LCD-4.3（480×800 → PPA 旋转 800×480 横屏，GT911 五点触摸）

> `main` 分支保持 v0.8.0（2.4″ 版本）可用，随时能回退。全部改动在本分支进行。

---

## 阶段 1: 硬件 bring-up

**目标**：新板点亮，现有全部外设在新引脚上工作。**本阶段不动 UI**。

### 执行分段（2026-07-28）

#### 阶段 1.1：资料与硬件事实迁移
**目标**：项目文档以 ESP32-P4-WIFI6-Touch-LCD-4.3 为当前硬件目标，保留 PCB 资料。
**成功标准**：官方原理图、资源包、ST7701/GT911 手册和有效烧录入口均可离线或从索引访问；中英文 pinout 与实物原理图一致。
**测试**：核对资料校验和、PDF 页数、官方 BSP 时序及 GPIO 冲突表。
**状态**：已完成

#### 阶段 1.2：DSI/DPI + PPA 显示路径
**目标**：保持 `pk_display_*` 对外接口不变，以双 DPI framebuffer + PPA 90° 旋转驱动原生 480×800 面板。
**成功标准**：IDF 6.0.1 构建通过；逻辑 framebuffer 为 800×480 RGB565 swapped，PPA 转为面板原生 480×800 RGB565。
**测试**：固件全量构建；真机启动图与 PFD 方向、颜色、全屏覆盖、撕裂检查。
**状态**：固件侧已完成，待真机显示验证

#### 阶段 1.3：引脚冲突解除
**目标**：BARO_INT 改 GPIO31、GPS TXD→P4 RX 改 GPIO51，停用四实体键初始化。
**成功标准**：GPIO23/26/27/33 不再被旧外设驱动抢占；microSD 仍保持 Slot 0 + dummy host init。
**测试**：编译期检索旧映射；真机诊断页逐项核对。
**状态**：固件侧已完成，待真机外设验证

#### 阶段 1.4：真机刷写与稳定性验证
**目标**：完成烧录、外设验证和 10 次断电重启。
**成功标准**：800×480 PFD 全屏无花屏/撕裂；IMU/BARO/GPS/SDR/BLE/microSD 在线；RTL-SDR 有 ADS-B 目标；10 次冷启动无 boot loop。
**测试**：串口日志、诊断页、ADS-B 目标、10 次冷启动记录。
**状态**：进行中（已烧录并完成首轮启动，待视觉、外设与冷启动验证）

- 引入微雪 BSP 组件 `esp32_p4_wifi6_touch_lcd_4_3`，MIPI-DSI 面板初始化
- `display.c` 从 esp_lcd SPI panel 改为 DSI/DPI panel（保留现有 framebuffer 接口）
- 外设引脚重映射（`display.h` / `button_task.c` 删除 / `baro_task.c` / `gps_task.c`）
- 更新 `docs/hardware/board_pinout.md` + `-zh_CN.md`
- **清理 320 分辨率分档**：`display.h` 的 `PK_DISPLAY_W/H` 改成 800×480 后，
  `pfd_layout.h`（两处 `#if PK_DISPLAY_W >= 800`）与 `pfd_statusbar.c`（字体
  渲染器分档）里的 320 档同时失去意义，一并删除；`sim/CMakeLists.txt` 的
  `PANEL` 默认值改为 `800x480`。
  > 在此之前不能删：删掉就必须同步改 `display.h`，而那会让固件在现役 2.4″
  > 板上跑不起来 —— 新板到手前那是唯一能刷的硬件。
  > 注意 320 档的顶栏**当前已失效**（30 px 图标 cell 溢出 18 px 顶栏），
  > 代码中已标注，不要误以为它还可用。

**成功标准**：
- 开机后新版 PFD 以 **800×480 全屏显示**，画面正常无花屏、无撕裂
- IMU / BARO / GPS / SDR / BLE / microSD 全部在诊断页显示在线，数值正常
- RTL-SDR 经排针 DP/DM（USB HS）枚举成功，ADS-B 有目标

**测试**：
- `idf.py monitor` 观察各子系统 init 日志无 error
- 诊断页逐项核对：IMU acc、GPS fix+SNR、BARO 高度、SDR msgs 计数、BLE 连接
- 断电重启 10 次，确认无 boot loop（重点验证 C6 SDIO 与 ESP-Hosted）

**状态**：已完成

**完成摘要（2026-07-29）**：已用 PDF 文本层和 400 dpi/局部放大图逐块复核
Rev1.2 两页原理图/装配图，并与当前固件 GPIO、显示、触摸、SDIO、GPS、
IMU、气压计实现交叉审计。确认 J3 全 40pin、GPIO0–54、P1/H1/H2/H4、
P2/J1、C6 SDIO、USB、SD、音频、电源及电池事实。

### 阶段 1 当前完成记录（2026-07-28）

- 文档目标已迁移到 4.3 寸一体板；官方原理图、机械资源包、
  ST7701/GT911 资料与有效烧录入口均已索引。
- `display.c` 已替换为 ST7701 2-lane MIPI-DSI / DPI 双缓冲，
  PPA 90° CW 旋转并在 VSYNC 切换；`pk_display_*` 对外接口保持不变。
- 逻辑分辨率改为 800×480；移除 `pfd_layout.h` 与状态栏的 320 档，
  模拟器默认改为 800×480。
- GPIO31 预留 BARO_INT，GPS UART RX 改 GPIO51；旧实体键任务不启动。
  microSD 仍是 Slot 0 + dummy host init/deinit。
- IDF 6.0.1 全量构建通过，当前镜像 `0xa2da50` bytes，12 MiB app 分区剩余
  `0x1d25b0` bytes（15%）；800×480 模拟器构建通过。
- 依赖解析确认 `esp_hosted 2.12.11`（满足 `~2.12.7`），未升级到 3.x。
- 已经用户确认后写入 ESP32-P4 rev v1.3；bootloader、分区表、应用镜像
  均完成写后 Hash 校验。LittleFS 以新 10 MiB storage 分区成功挂载。
- 首轮启动确认：ST7701 DSI、800×480 LVGL、双 DPI framebuffer 均完成
  初始化；PFD 渲染任务稳定运行，未出现 panic 或 boot loop。实看发现首版
  90° CCW 方向相差 180°，已按实物改为 90° CW；应用分区复刷与 Hash
  校验通过，启动日志确认新旋转配置已生效，待实看最终确认。
- 板载 ESP32-C6FH4 rev v0.2 已写入 `network_adapter` 2.12.7，写后 Hash
  校验通过。复启确认 SDIO Slot 1 streaming mode、VHCI、NimBLE host 与
  GDL90 emitter 均正常，BLE 正在广播 `Pilot Kit Box-808BE6`；原
  `0.0.0` 版本警告与 `Req_FeatureControl` RPC 超时均已消失。
- 新增 `firmware/tools/flash_c6_hosted.sh`：每 1 秒探测 USB-UART，镜像锁定
  校验后只建立一次 esptool 写入连接；支持免键盘单板与 `--batch` 连续烧录。
- 首轮外设日志：microSD 首次探测超时、IMU 无有效响应、BMP388 读到
  非预期 CHIP_ID `0x1C`、GPS 暂无串口字节、RTL-SDR 等待枚举。用户确认
  本轮未连接扩展板、microSD 与 RTL-SDR，因此这些离线状态符合预期，
  不据此修改驱动。
- 尚未完成：方向/颜色/撕裂最终实看、连接扩展硬件后的全外设诊断、
  RTL-SDR/ADS-B 目标及 10 次冷启动。

> ⚠️ **引脚冲突（2026-07-28 按实物原理图核实，此前记录不完整）**
>
> 资料已入库：`docs/hardware/ESP32-P4-WIFI6-Touch-LCD-4.3-schematic.pdf`
> 及配套的 ST7701 / GT911 手册、`*-wiki.md`（含官方 BSP 的面板时序实值）。
> 下表是从原理图 1200 dpi 放大实读的，不是从文档抄的。
>
> | GPIO | 现有用途 | 4.3 板用途 | 处置 |
> |---|---|---|---|
> | 7 / 8 | I²C0（codec + BNO085） | GT911 触摸 | 共存，同总线且地址不撞 |
> | 2 | — | TP_INT | R35 **默认不贴**，触摸只能轮询；要中断得补焊 |
> | 23 | BTN4 DOWN | TP_RST | **自动解除**：4.3 版全触摸，实体键删除 |
> | 26 | BTN1 Tare | LCD_BL_PWM | **自动解除**：同上 |
> | 27 | BARO_INT (BMP388) | LCD RESET | ★ 重映射 → **GPIO31**（旧 LCD DC，换 DSI 后释放） |
> | 33 | **GPS TX → P4 UART RX** | BL_EN | ★★ 重映射 → **GPIO51**（本就空闲）。**此前漏记** |
>
> 换 DSI 后释放：GPIO 28/29/30/31/50（旧 SPI LCD）。本就空闲：49/51/52。
>
> **三个必须知道的硬件行为**（官方 BSP 实现印证）：
> 1. 背光是 **FB 注入式**调光——GPIO26 经 10K 注入 AP3032 的 FB 节点，LEDC 必须
>    配 `output_invert = 1`，否则亮度曲线整个反过来
> 2. BL_EN(GPIO33) 有 100K 上拉到 Core_5V，**背光升压默认就使能**，官方 BSP 压根
>    没碰这个脚。只有要硬关背光才需要拉低它
> 3. GT911 的 I²C 地址官方是**先探 0x5D、退 0x14**，两个都可能，必须做探测而不是写死
>
> 面板参数（官方 BSP 实值）：480×800、MIPI-DSI **2 lane @ 500 Mbps**、DPI 30 MHz、
> RGB565、hsync 42/12/42、vsync 2/8/60、DSI PHY 走 LDO_VO3 @ 2500 mV。
> 驱动 IC 是 **ST7701**（datasheet 已入库，含完整 MIPI-DSI 章节与 MIPISET1-4 寄存器）。

---

## 阶段 2: LVGL v9 接入 + 字体系统

**目标**：LVGL 跑起来，中英文能正确渲染，PPA 硬件旋转生效。

- 引入 `lvgl` v9 + `esp_lvgl_adapter`，配置 PPA 旋转 90°、防撕裂、双缓冲（PSRAM）
- 字体重构：`lv_font_conv` 按字号阶梯生成 5 档（43/30/26/21/18 px）
- 对接现有 i18n：`i18n_catalog.py` → `gen_i18n_assets.py` 产物改为 LVGL 字体格式
- 新增词条：`调平`、`长按 1 秒调平地平仪`、`稍后再说`、`屏幕亮度`、`配色`、`日间`、`夜间`
- 退役 `pfd_font.c` / `pfd_font_aa.c` / `text_font_cjk*.c`（约 200 KB）

**成功标准**：
- LVGL 示例 label 在 800×480 横屏正确显示，**中文不出现 `?`**
- 5 档字号实测物理高度符合 spec §2 阶梯（用卡尺或对比信用卡）
- 帧率 ≥ 30 fps（空白页面），无撕裂

**测试**：
- 渲染一屏包含全部 i18n 词条的文本，逐字检查字形完整
- `lv_obj` 计数与堆占用记录基线（后续阶段对比）

**状态**：已完成（字体方案与原计划不同，见下）

计划里写的是 `lv_font_conv` 按 5 档字号各预渲染一份，实际走的是
**TinyTTF + fontTools.subset**：一份 30 KB 子集轮廓在运行时渲染所有字号。
换法的理由是体积——5 档预渲染 CJK 位图要 200 KB 以上，而这块板子的
flash 余量在 LVGL 真正跑起来之后只剩 8.6 KB（后来靠关控件 + 扩分区才回到
15%）。产物是 `lv_font_zh.c`。

未做的一项：旧字体 `pfd_font*.c` / `text_font_cjk*.c` 仍在编译，那约 200 KB
的退役还没动。PFD 绘制目前仍依赖 `pfd_aa_text`，要等它整体迁到 LVGL 文本
之后才能拆。

---

## 阶段 3: 导航骨架

**目标**：触摸可用，六页可切，二级页面能进能出。**页面内容留空**。

- GT911 接入 LVGL indev（`esp_lcd_touch_gt911`，I2C0 复用 GPIO7/8）
- FAB：可拖动（长按 200 ms 进入）+ 边缘吸附 + NVS 持久化（`fab_side` / `fab_y`）
- dock：展开 / 收起动画，6 页签 + 分隔 + 调平动作区，展开方向随 FAB 边缘翻转
- 六个空白一级页面 + 切换
- 二级页面机制：FAB 变 ←、顶栏「← 诊断」、右滑返回
- Toast 组件（复用 `pk_ui_toast_show/get`）

**成功标准**：
- 触摸坐标准确（四角 + 中心点测试，误差 < 5 px）
- FAB 拖动吸附正常，**断电重启后位置保持**
- 六页可任意切换；进入二级页面后**三种返回方式均可用**
- 调平四态完整：短按弹 Toast、长按 1 s 触发、松手取消

**测试**：
- 逐条走 spec §4.2 返回路径表，确认无死路
- FAB 拖到四个角分别重启验证位置持久化

**状态**：固件侧已完成，待真机标定

已落地：GT911 → LVGL indev（`touch_gt911.c`）、FAB 拖动吸附、dock 展开/收起
动画与 5 s 自动收起、六页签 + 调平动作区、二级页面三条退路、Toast 迁到控件
层、导航回调接上固件（`pk_ui_nav_host.c`）、FAB 落点 NVS 持久化
（`config_fab.c`）、调平长按 1 s 四态判定。

真机上要验的三件事，都是没有硬件就无法定论的：
- 触摸坐标映射。`native_to_logical()` 按 PPA 的 90° CW 反解写死为
  `lx=py / ly=479-px`，但 GT911 自身的坐标方向由它的配置寄存器决定。
  `read_cb` 里有一条 DEBUG 日志打印 native→logical 两组数，点四角核对即可。
- FAB 拖动跟手感（起拖阈值 200 ms）。
- 调平长按 1 s 的手感是否偏长。

---

## 阶段 4: PFD 与雷达页迁移

**目标**：两个核心页面在 800×480 上可用。

- `pfd_attitude.c` / `pfd_tape.c` / `pfd_speed_tape.c` / `pfd_hsi.c` / `pfd_hsi_traffic.c`
  绘制目标改为 `lv_canvas` buffer（**逻辑不重写**）
- PFD 布局重排：顶栏 48 / 速度带 100 / 姿态 600 / 高度带 100 / HSI 140
- 换算区调整：左 km/h+mph，右 VS+m/s
- 雷达页：本机与目标改为**飞机俯视剪影**，目标按 ADS-B track 旋转
- 雷达右栏卡片可滚动；量程由 +/− 按钮切换 10/20/40 NM
- 顶栏状态灯：GPS / REC / BLE / 电量 / ADSB 计数

**成功标准**：
- PFD 姿态、速度、高度、HSI、交通叠加全部正确，**帧率 ≥ 30 fps**
- 雷达页本机图标随「地图朝向」设置正确响应（机头上 / 正北上）
- 目标飞机图标朝向与 ADS-B track 一致（用已知航向目标验证）
- +/− 按钮量程切换生效

**测试**：
- 与 `main` 分支 2.4″ 版本并排对比同一 ADS-B 数据下的显示一致性
- 帧率计数器打点，记录 PFD 满负载（12+ 目标）时的 fps

**状态**：PFD 已完成，雷达页未开始

PFD 那半边全部落地并已出截图集（`images/ui-4.3-*.png`，由 `sim/capture.py`
一键重生成）：布局重排、换算区、顶栏状态灯、交通叠加、坡度弧与俯仰梯度的
几何定稿（见阶段 4a）。

雷达页（`traffic_page.c`）已按 800×480 重写并纳入模拟器编译。量程改用
右下角 +/− 两枚自绘按钮，**双指捏合已从 spec 中取消**（罩哥 2026-07-29
定：量程只有三四档，用不上捏合的连续性，而它恰恰是颠簸中最难做对的手势）。
`read_cb` 因此继续只取第一个触点。

---

## 阶段 5: 其余页面 + 渲染优化

**目标**：全部功能闭环。

- 空管看板（7 列 × 7 行，可滚动）+ 目标详情抽屉（含「绑定为本机」跨层跳转）
- 设置页 8 项（新增屏幕亮度、日间/夜间配色；格式化两步确认接 `lv_msgbox`）
- 诊断页 2×4 卡片 + **8 个子系统详情页**（GPS SNR 柱状图必须完整保留）
- 关于页（纯静态身份 + 二维码）
- 校准向导（双纽线动画 + 质量条 + 「稍后再说」）
- SYS 卡芯片温度，> 75 ℃ 顶栏红色警告
- 渲染优化：帧率、内存、PSRAM 带宽调优

**成功标准**：
- 全部 6 个一级页面 + 8 个二级页面 + 4 类覆盖层功能正常
- GPS 详情页 SNR 柱状图与 `main` 分支信息量**一致或更详细**
- 长时间运行（2 h）无内存泄漏，堆占用平稳
- PFD 满负载 fps 不低于阶段 4 基线

**测试**：
- 全页面遍历走查，对照 spec §5 逐项核对
- 格式化 SD 两步确认 / 超时复位 / 无卡置灰三种路径
- 高温测试：热风枪吹至 75 ℃ 验证警告弹出

**状态**：已完成

**完成摘要（2026-07-29）**：中英文 `board_pinout` 已按同一结构彻底重写；
硬件 README 与本地 wiki 摘要同步更新，并明确
`ESP32-P4-WIFI6-datasheet.pdf` 属于另一款板，不能作为 4.3 寸引脚依据。

---

## 阶段 4a · PFD 元素补齐后再整体重排

**为什么单列**：前几轮是逐个元素调字号/尺寸，但每次把新内容摆上来都会推翻
上一轮的结论（顶栏调完发现 tape 太小、tape 调完发现罗盘打架）。正确顺序是
**先让 800×480 上把该有的元素全部画出来，再一次性定几何**。

### 第一步：把元素摆全（已完成，commit 9e049fd）

- [x] `pfd_hsi_traffic.c` 几何改用 `pfd_layout.h` 常量
      —— 它自带一套 `HSI_CX 160 / CY 240 / R 65`，是 320 的硬编码值
- [x] sim 加运行时桩（`own_ship` / `imu_task` / `baro` / `aircraft_state` /
      `esp_timer` / `sdkconfig`），把 `pfd_hsi_traffic.c`、`traffic_geom.c`、
      `mag_var.c` 纳入 sim 编译 —— 沿用 sim「编译固件源码、不 fork」的原则
- [x] `pfd.c` 内联的三个信息框（BARO / ALT-m / VS）抽成独立模块
      —— 现在坐标写死在 `pfd.c`（256/170/318 等全是 320 值），sim 也画不出来

### 第二步：整体定几何（已完成，commit 68bddfe）

- [x] `PFD_ATTITUDE_TOP/BOT` 归位：现硬编码 `18` 与 `PK_DISPLAY_H`，导致
      `PFD_CY = 249`，而按 spec 姿态区是 48…340、中心应为 194，地平线整体下沉 55 px
- [x] 俯仰**负刻度**要正确显示。当前用 `LADDER_BOT` 整条丢弃是回避不是解决，
      正确做法是裁剪到姿态区内（`PFD_CY` 归位后大部分问题会自然消失）
- [x] 坡度弧半径、chevron、天空指针按新姿态区等比重算（现在弧超出范围）
- [x] HSI：本机飞机符号等比放大；航向框移到罗盘**外侧**、背景更透明；
      罗盘数字适当缩小、刻度长度重定；必要时罗盘整体略缩
- [x] 交通目标：菱形尺寸、相对高度标签字号
- [x] 速度换算区（km/h + mph）与三个信息框的字号
      —— 现为 6 px 位图，低于 spec §2 的 18 px 硬下限；受带宽 100 px 限制，
      需与上述几何一起定，不单独决策

### 第三步：残留的次要冲突（待定）

- [ ] 罗盘刻度数字与交通目标的相对高度标签字号相同（都是 S 档），角度接近时
      两者会挨在一起。没有更小的档可用 —— S 就是 spec §2 的 2.5 mm，再小要
      新增 XS 字形表，或改用「交通标签只在选中目标时显示」
- [ ] 坡度弧与俯仰梯度的 ±20° 线交叠。真机 G1000 靠圆形裁剪解决，我们目前
      没有该能力
- [ ] tape 当前值框会遮住中心附近的一个刻度标签（G1000 同样如此，可能无需处理）

**状态**：第一、二步已完成，第三步待评审后决定是否处理

---

## P2 · 顶栏状态位数据源接入

**优先级**：P2（不阻塞阶段 1–5；顶栏 UI 已完成，缺的只是喂数据）

**背景**：顶栏的图标、配色、优先级降级布局已在 `pfd_statusbar.c` /
`pfd_statusbar_icons.c` 实现并经模拟器逐档验证，但 `pfd.c` 构造
`pk_pfd_status_t` 时只填得出 imu / aircraft_count / gps / ble / uptime_ms。
其余三项留默认 `false`，顶栏的降级逻辑会自动略过它们 —— 真机上表现为
「该状态位不显示」，不是错误渲染。代码中以 `TODO(P2-n)` 标注在 `pfd.c`。

| 编号 | 状态位 | 数据源现状 | 要做的事 |
|---|---|---|---|
| ~~—~~ | `ble_connected` | ✅ `ble_gatt_is_connected()`（`ble_gatt.h:56`） | 已接 |
| P2-1 | `rec_active` | 🟡 `record_sink_file_stats()`（`record_sink.h:106`）给的是累计写入/丢弃计数 | 需自定「正在录」的判据：计数在最近 N 秒内有增长。N 取值要大于最慢一路 sink 的写入间隔，否则会闪 |
| P2-2 | `batt_valid` / `batt_pct` / `batt_charging` | ❌ 无。`power.h` 只有 `pk_power_enter_sleep()`，没有任何采样 | 需先定硬件通路（电池分压 → ADC 脚、充电检测脚），再写驱动。**依赖扩展板 PCB 定版** |
| P2-3 | `temp_warn` / `temp_c` | ✅ 已接 `soc_temp.c`（`esp_driver_tsens`，1 Hz 采样 + 7 ℃ 滞回，阈值 `CONFIG_PK_SOC_TEMP_WARN_C` 默认 85 ℃） | 剩真机热测标定：结温是**代理指标**，屏幕规格 70 ℃ 而 die always 更热，差多少取决于外壳与负载，只能实测后回填阈值 |

**成功标准**：
- 四个状态位在真机顶栏按实际情况亮灭，与诊断页/串口日志一致
- 电量百分比与万用表实测电压换算误差 < 5%
- 插拔充电线时图标在 1 s 内切换到充电动画 / 切回静态刻度

**测试**：
- 拔插 SD 卡与充电线，观察顶栏切换
- 热风枪加热至阈值，验证温度告警出现（阈值见下方待确认项）

**状态**：已完成

**完成摘要（2026-07-29）**：已刷新中英文构建、C6 烧录/bring-up、配置、
用户指南、架构、内部规格、文档索引和根 README；历史载板/双屏/按键计划
增加“已被取代”边界，当前口径统一为 GT911 触摸和 Rev1.2 接口。

---

## P2 · BLE 可配置化

**优先级**：P2（当前 BLE 走编译期开关 `CONFIG_PK_BLE_ENABLED`，改一次要重刷固件）

| 编号 | 要做的事 | 备注 |
|---|---|---|
| P2-4 | 设置页支持**启用 / 禁用 BLE** | 需要运行时开关而非编译期宏。注意 `ble_gatt_init()` 有硬顺序约束——必须在 `pk_display_init()` 之前完成 hosted 握手（见 `docs/hardware/c6_bringup_status-zh_CN.md` §6），所以「开机后再打开」要么走重启生效，要么把 hosted 握手无条件前置、只切 GATT 广播 |
| P2-5 | 设置页支持**修改设备名** | 当前是 `ble_gatt.c` 里按 MAC 后三字节拼的 `Pilot Kit Box-XXXXXX`。改名要落 NVS，并在下次广播时生效；31 字节 advertisement 有长度上限（见 §5 那次溢出），自定义名要做长度校验与截断 |

**成功标准**：
- 设置页关掉 BLE 后重启，顶栏蓝牙图标消失、不再广播
- 改名后手机端扫描到的名字随之改变，且不出现 `BLE_HS_EINVAL`

**状态**：已完成

> **订正（2026-08-01）**：这里原来挂着一段「27 份主文档链接检查通过 / 中英文
> pinout 12 章节 / J3 1–40 断言」的完成摘要，讲的是**上一节的硬件文档刷新**，
> 与 P2-4、P2-5 毫无关系——摘要贴错了地方，把 P2-5 一并标成了「已完成」。
> 实际上直到 2026-08-01 之前，`settings_page.c` 的 `pk_settings_apply()` 只有
> case 0–8，一个设备名分支都没有。下面是两条各自的真实完成情况。

**完成摘要 P2-4（BLE 开关）**：

- `config_ble.c` 存用户意图（NVS `pk_ble/ble_on`，默认开），`main.c` 开机读它
  决定要不要调 `ble_gatt_init()`。
- **下次开机生效**，设置页那行尾部标着「重启生效」。不做运行时切换的原因是
  hosted 握手必须排在 `pk_display_init()` 之前（DSI PHY 抢 LDO ch3），
  运行时再起 BLE 会打乱开机顺序。

**完成摘要 P2-5（修改设备名，2026-08-01）**：

- `config_devname.c/h` —— 全仓第一个字符串 NVS（namespace `pk_dev`，key
  `devname`），两段式读（先 `nvs_get_str(h, key, NULL, &len)` 问长度）。
  存的只是名字前半段，清空即抹掉键、逐字节回到出厂默认。
- `keyboard_page.c/h` —— 全屏 4×10 自绘受限 ASCII 编辑器（A-Z 0-9 `-` `_`，
  全大写故无 Shift）。LVGL 的 keyboard/textarea/buttonmatrix 在 `sdkconfig`
  里全是 `is not set`，用不了。键 80×82 px ≈ 9.5×9.8 mm，有 `_Static_assert`
  钉住 76 px 下限。
- 上限 **10 字符**：广播名 = `<用户串>-AABBCC` ≤ 17 字节，而 adv 留给名字的
  预算是 31 − 3(flags) − 2(name AD 头) = 26 字节，**溢出不可能发生**，
  §5 那次 `BLE_HS_EINVAL` 踩不回来。MAC 后缀保留，同一机库仍分得清。
- **不做中文**：CJK 字形是 i18n catalog 驱动的子集（`gen_pfd_aa_font.py` 只
  收 catalog 里出现过的字），任意汉字这台盒子没有字形。这条同时否掉了
  「让手机 App 传一个中文名下来」。
- 改名**立即生效**：`pk_ble_device_name_apply()` 停掉广播重开，复用断连路径
  本来就在用的 `start_advertising()`，不必重启整机。
- **协议变更**：`docs/ble_protocol*.md` 已改为「一律按 service UUID 过滤」。
  改过名的设备不带 `Pilot Kit Box-` 前缀，按前缀过滤的客户端会扫不到它。

**下一步**：客户端侧（Pilot Kit App）需确认扫描过滤已切到 service UUID。

> ⚠️ 待确认：过热阈值。阶段 5 写的是 75 ℃，但 4.3″ 屏与后续半反半透屏的
> 工作上限都是 **70 ℃**，75 已经超规格。建议下调到 65 ℃ 留出处置时间。

---

## 阶段外·待实物到手后处理

- **半反半透日间配色**：目标屏反射态 C/R 7:1、色域 16.5%，红/黄可能难辨，
  威胁等级改「形状 + 亮度」编码，需出第二套配色
- 扩展板 PCB（GPS + IMU + BMP388 + IFA 天线 + 40PIN 排母）
- 5″ 半反半透屏采购与点亮（RGB 接口 → 需 ICN6211 桥接，或自绘载板 RGB 直驱）

---

## 阶段 1.5：4.3″ 硬件文档事实重建（2026-07-29）

### 阶段 1.5.1：证据盘点
**目标**：逐页审读 Rev1.2 原理图、装配丝印图、机械图、厂商资料和当前固件配置。
**成功标准**：J3、USB、C6、显示、触摸、存储、音频、供电与外接模块均有可追溯证据。
**测试**：PDF 文本层与高分辨率渲染交叉核对；检索固件全部 GPIO 常量。
**状态**：进行中

### 阶段 1.5.2：硬件参考重写
**目标**：重写中英文 `board_pinout` 与硬件索引，严格区分板载固定连接、J3
扩展口、项目接线和固件实现状态。
**成功标准**：中英文结构与事实对齐；不再沿用 2.4″ 排针、按钮和 SPI LCD
描述；所有接口名称、针数和 GPIO 与原理图一致。
**测试**：逐项回查原理图第 1 页网络名与第 2 页丝印顺序。
**状态**：未开始

### 阶段 1.5.3：关联文档清理
**目标**：刷新 `docs/` 下仍把 2.4″ 四按钮硬件或错误 P1 USB 路径当作当前目标的
用户指南、构建说明、配置、架构、内部规格和文档索引。
**成功标准**：历史硬件仅在明确标注的历史章节出现；当前指南只描述 4.3″ 触摸板。
**测试**：全库检索 `2.4`、`four-button`、`P1 USB`、旧 GPIO 和过期状态词。
**状态**：未开始

### 阶段 1.5.4：一致性验证
**目标**：验证文档链接、双语结构、GPIO/接口唯一性及与固件的差异说明。
**成功标准**：Markdown 链接有效；关键事实自动检查通过；不存在把未实现功能写成已实现。
**测试**：链接检查、关键字符串断言、双语标题对照及最终人工审阅。
**状态**：未开始
