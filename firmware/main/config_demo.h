/*
 * config_demo.h — 演示模式开关，NVS 持久化。
 *
 * 演示模式把 IMU / GPS / 气压 / ADS-B 目标四路数据源整体换成 demo_data.c 的
 * 合成值，用于没接外设时在真机上核对各页的**正常态**版面与交互。
 *
 * ⚠ 这是航空设备，假数据被当成真数据是会害人的。所以这个开关不是一个普通的
 * 偏好项，它带着一组不可绕过的约束：
 *
 *   1. **默认关**，且只能从设置页显式打开——没有任何自动、隐式、远程的开启
 *      路径。config_ble.c 那边默认开，因为"BLE 关着"只是少了个功能；这边
 *      默认必须是关，因为"演示模式开着"是个危险状态。
 *   2. **开着就一定看得见**：pk_ui_nav.c 在控件层常驻一枚红色 DEMO 徽标 + 整
 *      屏红框，压在所有页面之上，用户无法只要假数据不要标识。开机 splash 另
 *      有一条红字提示（splash 早于 LVGL，盖不到控件层，必须单独画）。
 *   3. **不往外送**：ble_gatt.c 在演示模式下停发 GDL90 的 Ownship / Traffic
 *      报文。理由见那里的注释——GDL90 线上格式没有"这是模拟数据"这一位，
 *      手机 App 收到假目标与收到真目标完全无法区分。
 *
 * 为什么仍然持久化到 NVS：不持久化的话每次重启都要重开，演示的便利性就没了。
 * 正因为会跨重启存活，上面第 2 条里的"开机 splash 提示"才是必需的而不是锦上
 * 添花——用户重新上电时必须被告知设备仍在演示态。
 *
 * 为什么**不做**超时自动退出：自动退出等于"模式在用户不知情时自己变了"，
 * 而这正是航空人因里最危险的一类（mode confusion）。展会上连开一天是正常
 * 用法，中途突然切回真实数据、屏上读数一起变，比一直保持演示态更容易误导。
 * 取而代之的是常驻标识 + 停发 GDL90 + 每秒一条 WARN 日志，让"还开着"这件事
 * 在屏上、手机上、串口上三处都藏不住。
 */
#pragma once

#include <stdbool.h>

#ifdef __cplusplus
extern "C" {
#endif

/* 演示模式是否开启。热路径（每帧都问）——实现只读一个 volatile，不碰 NVS。 */
bool pk_demo_enabled(void);

/* 开 / 关并落 NVS。立即生效：各 getter 每次调用都重新判定，不缓存。 */
void pk_demo_set_enabled(bool on);

/* 开机时从 NVS 读取。须在 PFD 任务启动前调用。 */
void pk_config_demo_load(void);

#ifdef __cplusplus
}
#endif
