/*
 * demo_track_sd.h —— 从 SD 卡的 /sdcard/demo/ 读 GPX，替换演示模式的轨迹。
 *
 * 演示模式于是有两态，切换只靠"卡上有没有 .gpx"，没有开关也没有设置项：
 *
 *   默认   卡上没有 → 播内置的 ZGGG→ZBAA 全程轨迹（demo_track_data.c）
 *   自定义 卡上有   → 播 /sdcard/demo/ 里排序第一个 .gpx
 *
 * 这样展会上换演示空域、开发时构造特定区域的场景，都只要拷个文件，不用重烧。
 *
 * ── 为什么是后台任务而不是开机路径上同步做 ─────────────────────────────
 * 6084 点的解析 + 五条清洗 + 五量联合抽稀在 P4 上是**几百毫秒到一秒**量级
 * （double 在 P4 上是软浮点，dist_m 里的 hypot/cos 是大头）。摆进 app_main 就
 * 是白白给开机时间加一秒，而演示模式本来就有一张能立刻开播的内置表。所以：
 * 开机先播内置的，加载完了再原子切过去。
 *
 * 切换那一下本机位置会从 ZGGG 跳到新轨迹的起点，pk_win 的格集合会一次性全部
 * 失效重载——这正是 demo_track.h 里往返播放要避免的那种 IO 尖峰。区别在于它
 * **只在开机后几秒发生一次**，而且那会儿窗口本来就在做首次填充，没有稳态可
 * 污染；用一次性的重载换掉"开机路径上多等一秒"，划算。
 *
 * ── 拔卡 ───────────────────────────────────────────────────────────────
 * 不受影响，**照常播**。解析一结束就 fclose，之后本模块不持有任何 SD fd、
 * 没有在途 SD IO，轨迹表整个躺在 PSRAM 里。所以这里刻意**不注册**
 * pk_sdcard_register_pre_unmount_cb ——那四个槽位是给"卸载前必须静默自己的
 * SD IO"的模块用的（pk_aero_span / aircraft_db），本模块没有可静默的东西，
 * 占一个槽只会挤掉真正需要的人。
 */
#pragma once

#ifdef __cplusplus
extern "C" {
#endif

/* 用户放 GPX 的目录。与 /sdcard/aero（航空数据库）、/sdcard/maps（瓦片包）
 * 并列，不并进它们——那两个目录里是工具生成的二进制包，塞一个用户手拷的
 * GPX 进去，将来"这个目录能不能整个删了重灌"就没法回答了。 */
#define PK_DEMO_SD_DIR   "/sdcard/demo"

/*
 * 创建一次性的后台加载任务。零 IO、零阻塞返回，须晚于 pk_sdcard_init()。
 *
 * 任务行为：等挂载（每 2 s 一次，最多 60 s）→ 列目录 → 取排序第一个 .gpx →
 * 解析 → 成功就 pk_demo_track_use() 切过去 → 无论成败都退出。
 * 任何一步不成立都只是**回落内置轨迹**（那已经是当前状态，什么都不用做），
 * 演示模式不会因为卡的问题变得不可用。每一条分支都有一行 pk_demo_sd 日志。
 */
void pk_demo_track_sd_init(void);

#ifdef __cplusplus
}
#endif
